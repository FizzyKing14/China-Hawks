# The deterministic core (src/) is pure stdlib Python — no runtime deps.
# These are for development / running the kit.
pytest>=7.0
PyYAML>=6.0

# Only needed for web/app.py (the NBA trade balancer UI)
Flask>=3.0
